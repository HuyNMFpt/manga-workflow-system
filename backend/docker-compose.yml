version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: manga-mysql
    environment:
      MYSQL_ROOT_PASSWORD: 123456
      MYSQL_DATABASE: manga_swp391
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql

volumes:
  mysql-data: